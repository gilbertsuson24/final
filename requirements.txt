# Raspberry Pi 5 Object Detection Requirements
# Compatible with Debian Trixie and OV5647 camera module

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0

# TensorFlow Lite Runtime for ARM64 (Raspberry Pi 5)
# Note: Use tflite-runtime for better performance on Pi 5
tflite-runtime>=2.14.0

# Alternative: Full TensorFlow (if tflite-runtime has issues)
# tensorflow>=2.14.0

# System dependencies (install via apt on Debian Trixie)
# rpicam-apps (for camera access)
# libcamera-dev
# python3-opencv

# Optional: For better performance
# pillow>=9.0.0

# Development dependencies (optional)
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
