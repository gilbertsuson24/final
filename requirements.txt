# Raspberry Pi 5 Object Detection Requirements
# Compatible with Debian Trixie and OV5647 camera module

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0

# TensorFlow Lite Runtime for ARM64 (Raspberry Pi 5)
# Try tflite-runtime first (more efficient), fallback to full TensorFlow
# Option 1: tflite-runtime (preferred for performance)
tflite-runtime>=2.14.0

# Option 2: Full TensorFlow (fallback if tflite-runtime fails)
# tensorflow>=2.14.0

# If tflite-runtime installation fails, uncomment the tensorflow line above
# and comment out the tflite-runtime line

# System dependencies (install via apt on Debian Bookworm)
# rpicam-apps (preferred for OV5647 camera on Pi 5)
# libcamera-tools (fallback option)
# python3-opencv

# Optional: For better performance
# pillow>=9.0.0

# Development dependencies (optional)
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
