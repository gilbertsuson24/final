# Raspberry Pi 5 Object Detection Requirements
# Compatible with Debian Trixie and OV5647 camera module

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0

# TensorFlow Lite Runtime for ARM64 (Raspberry Pi 5)
# Note: tflite-runtime doesn't have ARM64 wheels for Python 3.13
# Using full TensorFlow instead for compatibility
tensorflow>=2.14.0

# Alternative: If you want to try tflite-runtime, use this instead:
# tflite-runtime>=2.14.0 --find-links https://github.com/google-coral/pycoral/releases/download/v2.0.0/tflite_runtime-2.14.0-cp313-cp313-linux_aarch64.whl

# System dependencies (install via apt on Debian Trixie)
# rpicam-apps (for camera access)
# libcamera-dev
# python3-opencv

# Optional: For better performance
# pillow>=9.0.0

# Development dependencies (optional)
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
